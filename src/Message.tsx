// PascalCaseing
function Message() {
  const name = "VOLT";
  if (name) {
    //JSX : JavaScript XML
    return <h1>HELLO {name}</h1>;
  } else {
    //JSX : JavaScript XML
    return <h1>HELLO HOOMANS</h1>;
  }
}

export default Message;
